__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.DS_Store

# a workspace's own output, if someone builds inside the checkout
capture/
.verba/
content/masking-map.json

# advisory locks, created at runtime
*.json.lock
